# remedik Helm chart

**Status: skeleton.** Templates land with the `add-mvp-core` OpenSpec
change; until then this chart is not installable.

`values.yaml` is the design contract for the v0.1.0 values schema and is
kept in sync with the specs. Keys marked `(v0.2.0)` belong to later changes.
